namespace SistemaNotas.Models;

// COMPOSICIÓN - Esta clase será utilizada exclusivamente como parte de un modelo mayor y no tiene sentido por sí sola en este dominio aplicativo
public class Direccion
{
    public string Calle { get; set; } = string.Empty;
    public string Ciudad { get; set; } = string.Empty;
}
